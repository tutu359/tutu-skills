## Agent skills

### Issue tracker

Issue 和 PRD 记录在 GitHub Issues 中；使用 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的规范标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

这是一个 single-context 仓库：在相关工作中读取根目录的 `CONTEXT.md` 和 `docs/adr/`。详见 `docs/agents/domain.md`。
